# PulseMotion TV – Environment Configuration
#
# Zero-Cloud Architecture:
# No external API keys or cloud secrets are required.
# All computer vision and pose tracking inference runs 100% on-device
# via WebAssembly (Wasm) and WebGL acceleration on Amazon Fire TV.

PORT=3000
NODE_ENV=development